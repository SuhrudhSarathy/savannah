defaults:
  - model: dit_block
  - task: pusht
  - _self_

overfit:
  batch_size: 8
  image_size: 96
  steps: 500
  lr: 1.0e-4
  log_every: 50
  loss_threshold: 0.01 # final loss must drop below this to count as a PASS
