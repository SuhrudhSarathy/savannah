_target_: savannah.tasks.metaworld_assembly.MetaworldAssemblyTask
config:
  _target_: savannah.data.dataset.DataSetConfig
  repo_id: suhrudhsarathy/metaworld-assembly-v3
  fps: 20
  cameras: ["topview", "gripperPOV", "behindGripper"]
  obs_horizon: 1
  action_horizon: ${model.action_horizon}
  image_size: 128
  batch_size: 32
  num_workers: 4
  train_fraction: 0.95
  cache_in_memory: true
