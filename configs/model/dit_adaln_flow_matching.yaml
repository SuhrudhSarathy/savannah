_target_: savannah.models.dit_adaln_policy.DiTAdaLNPolicy
embed_dim: 128
num_blocks: 4
encoder_num_attn_heads: 4
encoder_feedforward_dim: 512
encoder_dropout: 0.1
decoder_num_attn_heads: 4
decoder_feedforward_dim: 512
decoder_dropout: 0.1
state_dim: 2
action_dim: 2
action_horizon: 12
num_cameras: ${len:${task.config.cameras}}
objective:
  _target_: savannah.objectives.FlowMatchingObjective
  inference_steps: 10
vision_encoder:
  _target_: savannah.models.encoders.vision_encoder.VisionEncoder
  embed_dim: ${model.embed_dim}
  backbone:
    _target_: savannah.models.backbones.resnet_backbone.ResNetBackbone
    out_channels: 96
language_encoder: null
