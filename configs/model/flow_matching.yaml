_target_: savannah.models.flow_matching.FlowMatchingPolicy
embed_dim: 128
num_attn_heads: 4
feedforward_dim: 512
num_blocks: 6
state_dim: 2
action_dim: 2
action_horizon: 8
num_cameras: 1
objective:
  _target_: savannah.objectives.FlowMatchingObjective
  inference_steps: 10
vision_encoder:
  _target_: savannah.models.vision_encoder.VisionEncoder
  embed_dim: ${model.embed_dim}
  backbone:
    _target_: savannah.models.backbones.resnet_backbone.ResNetBackbone
    out_channels: 96
