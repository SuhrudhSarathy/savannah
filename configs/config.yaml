defaults:
  - model: dit_block
  - task: metaworld_assembly
  - trainer: default
  - _self_

wandb:
  project: savannah
  run_name: null
  enable_logging: true
  enable_model_checkpoint: false # upload best/best_success/latest checkpoints as W&B artifacts

hf:
  repo_id: null # e.g. set via CLI: hf.repo_id=username/my-policy
  push_to_hub: false

log_level: INFO # DEBUG | INFO | WARNING | ERROR
