defaults:
  - model: dit_adaln
  - task: pusht
  - trainer: default
  - _self_

wandb:
  project: savannah
  run_name: adaln_pusht
  enable_logging: true
  enable_model_checkpoint: false # upload best/best_success/latest checkpoints as W&B artifacts

log_level: INFO # DEBUG | INFO | WARNING | ERROR
